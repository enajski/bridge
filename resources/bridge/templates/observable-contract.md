Generate an `observable-contract` for `{{project-name}}`.

Target files:
{{files|bullets}}

Task:
- identify observables needed for conformance, traces, differential checks, or runtime assertions
- record trigger point, capture timing, snapshot strength, collection mode, semantics scope
- map observables to claims and evidence consumers
