Generate a `verification-brief` for `{{project-name}}`.

Target files:
{{files|bullets}}

Task:
- classify system/workflow category
- group concerns by mechanism family
- split model-checkable, test-verifiable, review-only, out-of-scope work
- name handoff artifacts and environment assumptions
