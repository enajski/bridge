Generate one `change-impact-report` per changed file for `{{project-name}}`.

Changed files:
{{files|bullets}}

Include affected contracts, formal modules, tests, and replayable revalidation commands.
