Generate a `spot-evidence-card` for `{{project-name}}`.

Target files:
{{files|bullets}}

Task:
- propose 1-3 small proof-oriented client tests
- state proof obligations and runtime observables
- flag likely specification gaps if proof fails
