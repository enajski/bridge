Generate one `namespace-bridge-card` for subsystem `{{subsystem.name}}` in `{{project-name}}`.

Relevant files:
{{files|bullets}}

Use profile globs and nearby docs/tests/formal modules. Cite exact paths and line ranges. Distinguish observed facts, inferred semantics, and open questions.
